import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    // primary: {
    //   main: purple[500],
    // },
    secondary: {
      main: "#9a9c9c",
    },
  },
});

export default theme;
